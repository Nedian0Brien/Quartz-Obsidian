#Statistics 
[[Machine Learning]]

1. L1 정규화 : 라쏘 회귀([[Lasso Regression]])
	가중치의 기본 수식에다 L1 norm 페널티를 추가하여 계산함 -> 가중치 값이 커지면 비용함수(손실함수)에 매우 큰 영향을 받게 됨
2. L2 정규화 : 리지 회귀([[Ridge Regression]])
	가중치의 기본 수식에다 L2 norm 페널티를 추가하여 계산함 -> 가중치 값이 커지면 비용함수(손실함수)에 매우 큰 영향을 받게 됨

